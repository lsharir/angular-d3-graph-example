import { ForceDirectedGraph } from './force-directed-graph.model';

describe('ForceDirectedGraph', () => {
  it('should create an instance', () => {
    expect(new ForceDirectedGraph()).toBeTruthy();
  });
});
